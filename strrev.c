#include<stdio.h>
#include<ctype.h>
#include<string.h>


void main(){
 char inputstr[] = "Mani Balan";
 char revarr[20];
 int len = strlen(inputstr);
 for (int i = 0; i < len; i++)
 {
    
    printf("%s",revarr);
 }
 
}